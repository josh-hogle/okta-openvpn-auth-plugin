// Package auth implements the 'auth' sub-command.
//
// The 'auth' command is used for actually performing the authentication to Okta. Optionally, it can be used to test
// authentication interactively as well as run in its normal "script plugin" mode.
package auth
